
// const validateForm = (user, password) => user.length > 8 && password.length > 8;

// const valRegister = (password) => password

// export default valRegister

const validForm = {
  validateLogin: function validateForm (user, password){ 
    return user.length > 8 && password.length > 8}, // will be accessed after import using validForm.validateLogin()
// next key: value, pair
}

export default validForm;